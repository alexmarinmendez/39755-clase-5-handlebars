<div>
    <h1>Hola {{user}}</h1>
    {{#if isAdmin}}
    <div>La lista de foods es:</div>
    <ul>
    {{#each foods}}
        <li>{{this.name}} (ARG$ {{this.price}})</li>
    {{/each}}
    </ul>
    {{else}}
    <p>No tienes permisos para ver la lista de foods</p>
    {{/if}}
</div>